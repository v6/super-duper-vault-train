{
    "db_name": "mariadb",
    "creation_statements": ["          CREATE USER '{{name}}'@'%' IDENTIFIED BY '{{password}}';", "GRANT SELECT ON *.* TO '{{name}}'@'%';"],
    "default_ttl": "1h",
    "max_ttl": "24h"
}
